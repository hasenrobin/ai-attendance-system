This project currently has no schema.sql, migrations, RLS scripts, or Supabase database dump committed.

Before production work continues, the following must be exported from Supabase and saved into the project:

1. public table columns
2. foreign keys
3. indexes
4. triggers
5. functions/RPC
6. RLS policies

Do not modify code.
Do not build features.
Save this documentation file only.
