# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Recognition worker secrets (SUPABASE_SERVICE_ROLE_KEY) — never commit.
recognition-worker/.env.worker

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
*.env
node_modules/
dist/
camera-proxy/*.exe
*.tar.gz

# Installer build artefacts — binaries downloaded at build time, never committed.
installer/build/
installer/output/

