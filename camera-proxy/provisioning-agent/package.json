{
  "name": "camera-provisioning-agent",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Local agent that auto-provisions RTSP cameras into the MediaMTX proxy (codec detection, path creation, HLS verification).",
  "engines": {
    "node": ">=18"
  },
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "onvif": "^0.8.1",
    "yaml": "^2.5.1"
  }
}
